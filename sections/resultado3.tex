\section{efeitos da presença de inibição recorrente em simulações}
\subsection{características de disparos de motoneurônios}
Com o intuito de analisar os disparos e o recrutamento dos MNs,
uma corrente
em função do tempo foi injetada no soma de todos esses neurônios. Essa
corrente $i$ é descrita pela seguinte equação:

\begin{equation}
    i(t)=40t/1000.
\end{equation}

No intervalo de 1 segundo mostrado na Figura \ref{fig:recruitCxO}, pode-se
perceber que a presença
das CRs resulta em padrões de disparos
distintos. Como esperado, as CRs acabam atrasando os disparos dos MNs e 
assim diminuem suas ocorrências.
Além disso, é possível notar que, em ambos os casos e de forma geral,
os MNs disparam de forma ordenada, ou seja, do menor para o maior.

\begin{figure}[ht]
	\center
	\includegraphics[scale=0.6]{recruitCxO.eps}
    \caption[Momentos de disparo dos somas de todos os motoneurônios simulados.]{
             Momentos de disparo dos somas de todos os motoneurônios simulados.
             O eixo das ordenadas representa os índices dos motoneurônios, 
             que são diretamente proporcionais ao tamanho dos mesmos, e o
             eixo das abcissas são os
             instantes de disparo. Os disparos dos motoneurônios com (pontos vermelhos)
             e sem (pontos pretos) células de Renshaw estão sobrepostos.
            }
	\label{fig:recruitCxO}
\end{figure}

Nesse aspecto, todavia, existem algumas ressalvas. A Figura
\ref{fig:recruitzoom} mostra com mais detalhes os instantes de disparos
de MNs sob o efeito da inibição recorrente. Como visto na Figura
\ref{fig:recruitzoom}\subref{fig:recruitzoom1}, alguns MNs maiores inicialmente
disparam praticamente ao mesmo tempo. Os menores, por
sua vez, ainda apresentam um comportamento de disparos similar,
mas não parecem estar tão sincronizados,
como visto na Figura \ref{fig:recruitzoom}\subref{fig:recruitzoom2}.
A maior ativação de CRs ocasionada por MNs do tipo FF e o efeito do
decaimento da inibição recorrente com a distância, acompanhado pelo 
respectivo atraso nos disparos dos MNs, podem explicar esse resultado,
mas levantam questionamentos sobre a forma como o circuito de 
inibição recorrente distribui suas entradas pelo núcleo motor e a ordem
de recrutamento deste sob essas condições.

\begin{figure}[ht]
    \centering
    \subfloat[][]{
        \label{fig:recruitzoom1}
        \includegraphics[scale=0.5]{recruitzoom1.eps}
    }
    \hspace{-1cm}
    \subfloat[][]{
        \label{fig:recruitzoom2}
        \includegraphics[scale=0.5]{recruitzoom2.eps}
    }
    \caption[Visão de um intervalo reduzido dos disparos dos motoneurônios.]{
             Visão de um intervalo reduzido dos disparos dos motoneurônios.
             \subref{fig:recruitzoom1} Instantes de disparos de alguns
             motoneurônios do tipo FF. \subref{fig:recruitzoom2}
             Instantes de disparos de alguns motoneurônios do tipo S.
         }
    \label{fig:recruitzoom}
\end{figure}

Para estudar isso, uma configuração de simulação
um pouco mais realista foi utilizada. De forma semelhante à
corrente injetada usada anteriormente, o núcleo motor é ativado
por uma entrada excitatória que aumenta com o tempo de simulação,
mas, nesse caso, isso se dá por meio de
fibras descendentes disparando potenciais de ação com intervalos 
médios de disparos de acordo com um processo pontual Gama de ordem 7.
Sua frequência de disparos $FD$ é dada por

\begin{equation}
FD(i)=1500t/1000.
\end{equation}

Esses resultados são mostrados na Figura \ref{fig:recruitpoisson}.
Pode-se observar que, na presença de CRs
(Figura \ref{fig:recruitpoisson}\subref{fig:recruitpoissonC}),
o comportamento descrito 
no parágrafo anterior diminui, mas ainda pode ser observado.
A variabilidade nos disparos causada pela ativação utilizada
também parece ter ocasionado algo parecido na ausência das 
CRs, como visto na Figura
\ref{fig:recruitpoisson}\subref{fig:recruitpoissonO}, mas de forma
mais sutil.

\begin{figure}[ht]
    \centering
    \subfloat[][]{
        \label{fig:recruitpoissonO}
        \includegraphics[scale=0.5]{recruitpoissonO.eps}
    }
    \hspace{-1cm}
    \subfloat[][]{
        \label{fig:recruitpoissonC}
        \includegraphics[scale=0.5]{recruitpoissonC.eps}
    }
    \caption[Disparos de motoneurônios ativados por fibras descendentes.]{
             Disparos de motoneurônios ativados por fibras descendentes.
             Os retângulos desenhados sobre o gráfico mostram casos em que
             motoneurônios de diferentes tamanho disparam praticamente
             ao mesmo tempo.
             \subref{fig:recruitpoissonO} Simulação sem células de Renshaw.
             \subref{fig:recruitpoissonC} Simulação com células de Renshaw.
         }
    \label{fig:recruitpoisson}
\end{figure}

É de se esperar que essas características apresentadas tenham um
impacto na sincronia dos MNs. Esse aspecto, entretanto, será abordado
na seção \ref{sec:forca}. Por hora, convém analisar a ordem de 
recrutamento dos MNs simulados.

Na Figura \ref{fig:revrecruit}, a região de transição entre MNs do tipo
S para o tipo FR é mostrada. Tomando o instante do primeiro disparo 
do índice 78 como referência,
é possível identificar casos em que ocorreram uma inversão na ordem de 
recrutamento, ou seja, MNs maiores começam a disparar antes de menores.
Também existiram casos de MNs sendo recrutados ao mesmo instante, como
é o caso do 78 com o 64. Ocorrências
semelhantes podem ser encontradas em outras partes do gráfico, mas não
foram mostradas para não prejudicar a visualização deste resultado.

\begin{figure}[ht]
	\center
	\includegraphics[scale=0.6]{revrecruit.eps}
    \caption[Inversão na ordem de recrutamento de motoneurônios.]{
             Inversão na ordem de recrutamento de motoneurônios.
             Motoneurônios do tipo S (abaixo de 75) e do tipo FR
             (acima de 75) são separados por uma linha horizontal
             pontilhada. Uma linha vertical tracejada foi traçada sob o
             instante do primeiro disparo do motoneurônio 78 e 
             linhas horizontais sólidas representam o quanto algumas
             unidades menores estiveram atrasadas.
            }
	\label{fig:revrecruit}
\end{figure}
 
Apesar de uma vasta literatura confirmando o princípio do tamanho, existem
relatos de que, em algumas situações, esse padrão pode não ser rigorosamente
reproduzido \cite{heckman12}. Uma contração balística, que se assemelha
à simulação em questão, é um desses casos. Nesse contexto, acredita-se
que um recrutamento invertido ou misto esteja associado à mecanismos 
do sistema nervoso central responsáveis por conformar a força de acordo
com a contração muscular \cite{bawa14,allen18}.

Estudos de computacionais
mostraram que variabilidade das propriedades de MNs podem
reproduzir esse comportamento no músculo gastrocnêmios medial do gato
\cite{allen18}. Esses características não estão presentes nas simulações
realizadas aqui, indicando que as CRs podem, também, ser um mecanismo para
introduzir uma inversão na ordem de recrutamento dos MNs, mesmo que
não seja de uma forma global. De fato, a forma como o 
PIPS recorrente é distribuído pelo núcleo motor é um indício disso
\cite{wakeling06}, especialmente em regiões de transição entre tipos de MNs
(S para FR e FR para FF).

Isso poderia ser uma vantagem em situações
de contração muscular rápida, pois \textit{twichtes} maiores contribuiriam
nos instantes inicias de força muscular mais baixa.

Apesar de não ter sido mencionada, a taxa de disparos dos MNs é
importante para compreender fisiologicamente a relevância dos resultados
e será analisada na próxima seção.

\subsection{taxa de disparos de motoneurônios}
Além das características de recrutamento, o controle da força muscular depende
do disparo de unidades motoras, mas existem diferentes enfoques sobre esse
assunto que ainda carecem de investigação \cite{piotrkiewicz17}.

Esse
é o caso de um padrão conhecido como \textit{onion skin}, em que
unidades motoras recrutadas com limiares mais baixos mantém uma taxa de
disparo maior do que as com limiares maiores. Isso causa um aninhamento
de curvas de taxa de disparo, criando o aspecto de uma casca de cebola
\cite{deluca94}. Existem também situações em que se observa uma inversão
desse fenômeno (curvas se cruzando), configurando a \textit{onion skin}
invertida. Estudos computacionais mostram ambos podem ter seus
benefícios e limitações \cite{hu14,deluca15}.

A Figura \ref{fig:onion} mostra o resultado de uma simulação
em que se tenta reproduzir algum desses padrões com e sem a CR.
Pode-se perceber uma característica mista, em que a maioria se
comporta de acordo com o padrão \textit{onion skin}, mas também
existem cruzamento de curvas de MNs maiores com os menores. A
comparação da Figura \ref{fig:onion}\subref{fig:onionwithoutRC}
com a Figura \ref{fig:onion}\subref{fig:onionwithRC} mostra
que a presença de inibição recorrente não causa mudanças 
visíveis nessa característica de disparo dos MNs.

\begin{figure}[ht]
    \centering
    \subfloat[][]{
        \label{fig:onionwithoutRC}
        \includegraphics[scale=0.5]{onionwithoutRC.eps}
    }
    \hspace{-1cm}
    \subfloat[][]{
        \label{fig:onionwithRC}
        \includegraphics[scale=0.5]{onionwithRC.eps}
    }
    \caption[Taxa de disparo de MNs em função da taxa de disparo de
             fibras descendentes.]{
             Taxa de disparo de MNs em função da taxa de disparo de
             fibras descendentes. O mapa de cores identifica os
             motoneurônios de acordo com o seus índices.
             \subref{fig:onionwithoutRC} Sem célula de Renshaw.
             \subref{fig:onionwithRC} Com célula de Renshaw.
         }
    \label{fig:onion}
\end{figure}

A Figura \ref{fig:oniondvdt} mostra a derivada de uma das curvas mostradas
na Figura \ref{fig:onion}, escolhida ao acaso. Pode-se observar uma
maior diferença entre as derivadas para algumas faixas de disparos
das fibras descendentes.

\begin{figure}[ht]
	\center
	\includegraphics[scale=0.6]{oniondvdt.eps}
    \caption[Derivada da taxa de disparos de motoneurônios.]{
             Derivada da taxa de disparos de motoneurônios.
            }
	\label{fig:oniondvdt}
\end{figure}

A variabilidade de torque e EMG observada em certos níveis de
flexão plantar pode estar associada com a variabilidade nos 
disparos de fibras descendentes \cite{watanabe13}. Com o intuito
de verificar se isso poderia influir de alguma forma no resultado
apresentado nessa sessão, a ordem do processo Gama foi decrescida
de 7 até 1, respeitando as faixas correspondentes para cada
MVC descritas em \citeonline{watanabe13}. Como visto na Figura
\ref{fig:onion2}, esse aumento de variabilidade teve efeitos
evidentes no padrão de disparos dos MNs.

\begin{figure}[ht]
    \centering
    \subfloat[][]{
        \label{fig:onionwithoutRC2}
        \includegraphics[scale=0.5]{onionwithoutRC2.eps}
    }
    \hspace{-1cm}
    \subfloat[][]{
        \label{fig:onionwithRC2}
        \includegraphics[scale=0.5]{onionwithRC2.eps}
    }
    \caption[Taxa de disparo de MNs em função da taxa de disparo de
             fibras descendentes com ordem de processo pontual
             variável.]{
             Taxa de disparo de MNs em função da taxa de disparo de
             fibras descendentes com ordem de processo pontual
             variável. O mapa de cores identifica os
             motoneurônios de acordo com o seus índices.
             \subref{fig:onionwithoutRC2} Sem célula de Renshaw.
             \subref{fig:onionwithRC2} Com célula de Renshaw.
         }
    \label{fig:onion2}
\end{figure}

Em um esteira, à velocidade de 0.5 ms/s,
taxas de disparos de MNs dos músculos das patas traseiras
de gatos estão contidas no intervalo de 15 até 35 pps, aproximadamente
\cite{hoffer87}. Paralelamente, \citeonline{walmsley78} sugeriram que,
nessa velocidade, a porcentagem da força tetânica realizada está por
volta de 8\%.

Esses estudos são importantes para caracterizar
diferentes tipos de movimentos. Nos resultados de simulação apresentados,
nota-se que muitos MNs alcançaram taxa de disparos relativamente alta.
Entretanto, em movimentos como o pulo vertical, contrações próximas
à 100\% do MVC são atingidas \cite{walmsley78} e isso envolveria
maiores taxas de disparos de alguns MNs. Além disso, dentro de períodos
de tempo longos, foi observado que a média de disparos de MNs é de
131 $\pm$ 45 pps para sua faixa secundária e 53 $\pm$ 19 para sua
faixa secundária \cite{kernell65}.



\subsection{situações de força fásica?}
\label{sec:danger}
\subsection{força}
\label{sec:forca}
